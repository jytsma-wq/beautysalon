/**
 * Top-level config re-export.
 * Import from here rather than from individual lib files when you need
 * a single, stable entry-point for app configuration.
 */
export { SALON_INFO, locales, defaultLocale, LOCALE_NAMES, RTL_LOCALES, PRICE_RANGE } from './lib/constants';
